{
  "builds": [
    {
      "src": "apps/xulf/package.json",
      "use": "@vercel/node",
      "config": {
        "buildCommand": "npx nx build xulf --prod"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/apps/xulf/$1"
    }
  ]
}
