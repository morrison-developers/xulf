import {{pascalCase name}} from './{{pascalCase name}}';       // Preview component for {{pascalCase name}}
import {{pascalCase name}}Editor from './{{pascalCase name}}Editor'; // {{pascalCase name}}Editor component for the editor mode
import { editorMeta } from './editor';
import { functionMeta } from './functions';

/**
 * {{pascalCase name}} module definition.
 * 
 * This ties together the renderable component, its editor configuration,
 * and its available function graph inputs/outputs.
 */
export const {{pascalCase name}}Def = {
  type: '{{camelCase name}}',          // The module's type identifier
  component: {{pascalCase name}},      // The regular component for preview
  editorComponent: {{pascalCase name}}Editor,  // The editor-specific component for the editor
  editorMeta,                         // Editor-related metadata (e.g., editable props)
  functionMeta,                       // Function graph metadata
};
